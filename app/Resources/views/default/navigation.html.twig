<nav class="navbar navbar-default navbar-fixed-top">
    <img src="{{ asset('img/logo-colsubsidio.png') }}" class="logo-centered hidden-xs hidden-sm" alt="Colsubsidio">

    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <img src="{{ asset('img/logo-game.png') }}" class="navbar-brand" alt="Trivia Mundialista">
        </div>

        <div id="navbar" class="collapse navbar-collapse clearfix">
            <ul class="nav navbar-nav text-center">
                {% if path(app.request.get('_route'), app.request.get('_route_params')) != '/' %}
                    <li>
                            <a href="{{ path('homepage') }}">
                                {{ 'Mecánica'|trans({}, 'FOSUserBundle') }}
                            </a>
                            <br>
                    </li>
                {% endif %}

                {% if not is_granted("IS_AUTHENTICATED_FULLY") %}
                    {% if path(app.request.get('_route'), app.request.get('_route_params')) == '/login' %}
                        <li>
                            <a href="{{ path('fos_user_registration_register') }}">
                                {{ 'Crear mi cuenta'|trans({}, 'FOSUserBundle') }}
                            </a>
                        </li>
                    {% elseif path(app.request.get('_route'), app.request.get('_route_params')) == '/register/' %}
                        <li>
                            <a href="{{ path('fos_user_security_login') }}">
                                {{ 'Ya tengo una cuenta'|trans({}, 'FOSUserBundle') }}
                            </a>
                        </li>
                    {% else %}
                        <li>
                            <a href="{{ path('fos_user_registration_register') }}">
                                {{ 'Crear mi cuenta'|trans({}, 'FOSUserBundle') }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('fos_user_security_login') }}">
                                {{ 'Ya tengo una cuenta'|trans({}, 'FOSUserBundle') }}
                            </a>
                        </li>
                    {% endif %}
                {% else %}
                    {# <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Mi Cuenta <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            {% if path(app.request.get('_route'), app.request.get('_route_params')) != '/profile/' %}
                                <li>
                                    <a href="{{ path('fos_user_profile_show') }}">Mis Datos</a>
                                </li>
                            {% endif %}
                            <li>
                                <a href="{{ path('fos_user_security_logout') }}">Cerrar Sesión</a>
                            </li>
                        </ul>
                    </li> #}
                    <li class="pull-right">
                        <a href="{{ path('fos_user_security_logout') }}">Cerrar Sesión</a>
                    </li>
                {% endif %}
                <li>
                    <a href="#" id="buyModalBtn">
                        {{ 'Compra aquí'|trans({}, 'FOSUserBundle') }}
                    </a>
                </li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
